{
  "name": "ineffable",
  "version": "1.0.0",
  "private": true,
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "nuxt clean && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint:fix": "eslint --fix --ext \".ts,.js,.vue\" --ignore-path .eslintignore .",
    "release:pre": "yarn version --prerelease && git push && git push --tags",
    "release:major": "yarn version --major && git push && git push --tags",
    "release:minor": "yarn version --minor && git push && git push --tags",
    "release:patch": "yarn version --patch && git push && git push --tags"
  },
  "dependencies": {
    "nuxt-security": "^0.10.1"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "@nuxtjs/google-fonts": "3.0.0-1",
    "@nuxtjs/i18n": "^8.0.0-beta.6",
    "@types/node": "^18.11.18",
    "eslint": "^8.29.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-nuxt": "^4.0.0",
    "eslint-plugin-prettier": "^4.2.1",
    "nuxt": "3.1.0",
    "nuxt-purgecss": "^2.0.0",
    "nuxt-swiper": "^0.1.9",
    "prettier": "^2.8.1",
    "sass": "^1.56.1",
    "typescript": "^4.9.4"
  }
}
